# Never ship local environment files — the deployment gets its config from
# Vercel project environment variables. A stray .env here would override them
# (notably AUTH_URL, which must be inferred from the request host in production).
.env
.env.*
!.env.example

node_modules
.next
/src/generated
.agents
.windsurf
.claude
skills-lock.json
